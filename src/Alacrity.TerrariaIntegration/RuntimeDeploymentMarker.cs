namespace AlacrityTerraria
{
    // This project owns only an explicit MSBuild deployment target. The marker keeps it a valid
    // buildable SDK project without introducing a runtime assembly dependency.
    internal static class RuntimeDeploymentMarker
    {
    }
}
